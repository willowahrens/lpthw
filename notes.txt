mkdir dir: create directory called "dir"
cd dir: go to directory called "dir"

git add -A : add all unversioned files to current tree
git commit -m "commit message; say something descriptive here"
git push: push commits to remote repository

git log: show commit history